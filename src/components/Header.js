import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header className="header">
            <h2>Smart Spotlight</h2>
        </header>
    );
};

export default Header;